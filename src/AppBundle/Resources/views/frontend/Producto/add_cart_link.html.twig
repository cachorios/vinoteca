{%  if entity.stock>0  %}
    <button  class="btn btn-cart" data-cart-add="add_to_cart" data-link="{{ path("addtocart", {'id': entity.id}) }}" >
        {{ 'Comprar' | trans }}
        <i class="fa fa-shopping-cart"></i>
    </button>
{% endif %}